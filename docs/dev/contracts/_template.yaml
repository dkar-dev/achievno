id: TEMPLATE_CONTRACT_ID
 title: Replace this with a concise implementation title
priority: P0
status: draft

objective: >
  Describe the exact outcome expected from this bounded implementation task.

context:
  product: Achievno
  runtime:
    frontend: Next.js
    backend: Django + Django REST Framework
    database: existing PostgreSQL external dependency
    container_runtime: Podman rootless
  architecture:
    style: modular_monolith_practical_hexagonal
    bounded_contexts:
      - accounts
      - profiles
      - friends
      - groups
      - achievements
      - challenges
      - notifications
      - platform
    api_style:
      reads: screen_oriented_aggregate_payloads
      writes: command_endpoints
    db_access: Django ORM unmanaged models
    db_ownership: existing_schema_baseline

scope:
  include: []
  exclude: []

allowed_files: []

forbidden:
  - do not expand scope beyond this contract
  - do not modify PostgreSQL schema unless explicitly requested
  - do not add raw SQL outside infrastructure/db_functions or SQL patch files
  - do not put ORM queries directly inside DRF views
  - do not reintroduce FastAPI
  - do not add silent mock fallback for core pre-prod screens

implementation_notes: []

acceptance:
  commands: []
  manual_checks: []

output_required:
  - summary
  - changed_files
  - commands_run
  - known_gaps
  - blockers_if_any
